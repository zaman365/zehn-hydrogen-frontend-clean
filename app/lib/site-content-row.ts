/**
 * Shared horizontal content row — aligns navbar, trust strip, homepage bands (REQ-0007).
 * Sync with Header nav shell width; max 1400px centered.
 */
export const ZEHN_SITE_CONTENT_ROW =
  'w-full max-w-[1400px] mx-auto px-2 sm:px-4 lg:px-8' as const;
