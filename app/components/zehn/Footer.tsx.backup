
import { Link } from "react-router"
import { Instagram, Facebook, Twitter, Linkedin, Youtube } from "lucide-react"

const footerLinks = {
  shop: [
    { name: "All Products", href: "/collections/all" },
    { name: "Polo Shirts", href: "/collections/polo-shirts" },
    { name: "Cargo Pants", href: "/collections/cargo-pants" },
    { name: "Chino Pants", href: "/collections/chino-pants" },
    { name: "Winter Jackets", href: "/collections/winter-jackets" }
  ],
  about: [
    { name: "Our Story", href: "/pages/about" },
    { name: "Sizing Guide", href: "/pages/sizing" },
    { name: "Fabric Care", href: "/pages/care" },
    { name: "Press", href: "/pages/press" }
  ],
  support: [
    { name: "Contact Us", href: "/pages/contact" },
    { name: "FAQ", href: "/pages/faq" },
    { name: "Shipping & Returns", href: "/pages/shipping" },
    { name: "Track Order", href: "/pages/track" }
  ]
}

export function Footer() {
  return (
    <footer className="bg-foreground pt-16 sm:pt-20 lg:pt-24 pb-8 sm:pb-10 relative overflow-hidden text-primary-foreground">
      {/* Giant Background Text - Responsive sizing */}
      <div className="absolute bottom-0 left-1/2 -translate-x-1/2 pointer-events-none select-none z-0 overflow-hidden w-full flex justify-center">
        <span className="font-headline text-[120px] sm:text-[200px] md:text-[300px] lg:text-[400px] font-black text-primary-foreground/10 whitespace-nowrap leading-none tracking-tight-4">
          ZEHN
        </span>
      </div>
      
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 sm:gap-10 mb-12 sm:mb-16">
Tool call argument 'replace' pruned from message history.

          {/* Shop Links */}
          <div>
            <h3 className="font-sans text-body tracking-[0.3em] uppercase text-primary-foreground mb-3 sm:mb-4 font-medium">Shop</h3>
            <ul className="space-y-2 sm:space-y-3">
              {footerLinks.shop.map((link) => (
                <li key={link.name}>
                  <Link
                    to={link.href}
                    className="font-sans text-body text-primary-foreground/70 hover:text-accent transition-all duration-300 inline-block py-1"
                  >
                    {link.name}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* About Links */}
          <div>
            <h3 className="font-sans text-body tracking-[0.3em] uppercase text-primary-foreground mb-3 sm:mb-4 font-medium">About</h3>
            <ul className="space-y-2 sm:space-y-3">
              {footerLinks.about.map((link) => (
                <li key={link.name}>
                  <Link
                    to={link.href}
                    className="font-sans text-body text-primary-foreground/70 hover:text-accent transition-all duration-300 inline-block py-1"
                  >
                    {link.name}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Support Links */}
          <div>
            <h3 className="font-sans text-body tracking-[0.3em] uppercase text-primary-foreground mb-3 sm:mb-4 font-medium">Support</h3>
            <ul className="space-y-2 sm:space-y-3">
              {footerLinks.support.map((link) => (
                <li key={link.name}>
                  <Link
                    to={link.href}
                    className="font-sans text-body text-primary-foreground/70 hover:text-accent transition-all duration-300 inline-block py-1"
                  >
                    {link.name}
                  </Link>
                </li>
              ))}
            </ul>
          </div>
        </div>

        {/* Bottom Bar */}
        <div className="pt-8 sm:pt-10 border-t border-border/30">
          <div className="flex flex-col sm:flex-row items-center justify-between gap-3 sm:gap-4">
            <p className="font-sans text-body text-primary-foreground/60 text-center sm:text-left">
              © {new Date().getFullYear()} ZEHN. All rights reserved.
            </p>
            <div className="flex gap-4 sm:gap-6">
              <Link to="/pages/privacy" className="font-sans text-body text-primary-foreground/60 hover:text-accent transition-all duration-300">
                Privacy Policy
              </Link>
              <Link to="/pages/terms" className="font-sans text-body text-primary-foreground/60 hover:text-accent transition-all duration-300">
                Terms of Service
              </Link>
            </div>
          </div>
        </div>
      </div>
    </footer>
  )
}
